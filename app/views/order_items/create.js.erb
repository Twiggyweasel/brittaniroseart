<% if @order.errors.any? || @order_item.errors.any? %>
alert("not valid.")
<% else %>
  $(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
  $("#cart-badge").addClass("bounce");
    setTimeout(function() { $("#cart-badge").removeClass("bounce") }, 500);
<% end %>